/**
 * Bucket for raw videos uploaded by user.
 */
export const rawVideoBucketName = "vidspark-raw-videos";

/**
 * Bucket for processed videos that are ready to be viewed by users.
 */
export const processedVideoBucketName = "vidspark-processed-videos";

/**
 * Bucket for videos thumbnails.
 */
export const videosThumbnailsBucketName = "vidspark-videos-thumbnails";
